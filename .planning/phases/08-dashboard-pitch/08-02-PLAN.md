---
phase: 08-dashboard-pitch
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - components/dashboard/lead-pitch-box.tsx
  - lib/outreach.ts
autonomous: true
requirements:
  - PITCH-03
  - PITCH-06
  - PITCH-07
  - PITCH-08
  - PITCH-09

must_haves:
  truths:
    - "Pitch text can be copied locally from the dashboard"
    - "WhatsApp deep links use normalized Brazilian phone numbers and encoded pitch text"
    - "Email links include an encoded subject and body built from the current pitch"
  artifacts:
    - path: "lib/outreach.ts"
      provides: "Outreach normalization and deep-link builders"
      contains: "normalizeBrazilianWhatsAppNumber"
    - path: "components/dashboard/lead-pitch-box.tsx"
      provides: "Copy and send actions"
      contains: "handleCopyPitch"
---

<objective>
Turn the generated pitch into ready-to-use outreach actions from the detail panel.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add copy and send actions</name>
  <files>components/dashboard/lead-pitch-box.tsx</files>
  <action>
Expose copy, WhatsApp, and email actions that are enabled only when the needed data is present and show inline feedback where appropriate.
  </action>
</task>

<task type="auto">
  <name>Task 2: Normalize WhatsApp numbers and build deep links</name>
  <files>lib/outreach.ts</files>
  <action>
Implement Brazilian number normalization, including the 9th-digit rule for DDDs 11–28, plus helpers for `wa.me` and `mailto` URLs.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/08-dashboard-pitch/08-02-SUMMARY.md`
</output>
