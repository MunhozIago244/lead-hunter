---
phase: 08-dashboard-pitch
plan: 01
type: execute
wave: 1
depends_on:
  - 07-02
files_modified:
  - components/dashboard/lead-detail-panel.tsx
  - components/dashboard/lead-pitch-box.tsx
autonomous: true
requirements:
  - PITCH-01
  - PITCH-02
  - PITCH-04
  - PITCH-05

must_haves:
  truths:
    - "The detail panel renders a pitch box with placeholder text when no pitch exists"
    - "Pitch generation calls POST /api/pitch and updates local lead state"
    - "Re-generation overwrites the current pitch in the UI"
  artifacts:
    - path: "components/dashboard/lead-pitch-box.tsx"
      provides: "Pitch UI and generation logic"
      contains: "handleGeneratePitch"
---

<objective>
Expose server-side pitch generation in the dashboard and keep the selected lead synchronized after each generation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated pitch box component</name>
  <files>components/dashboard/lead-pitch-box.tsx, components/dashboard/lead-detail-panel.tsx</files>
  <action>
Render a new pitch section inside the detail panel that shows either the stored pitch text or a placeholder, plus a generate/re-generate button.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire generation to POST /api/pitch</name>
  <files>components/dashboard/lead-pitch-box.tsx</files>
  <action>
Call the existing pitch route, merge the returned text into the selected lead locally, and show inline loading/success/error feedback.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/08-dashboard-pitch/08-01-SUMMARY.md`
</output>
