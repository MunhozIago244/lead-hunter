---
phase: 06-dashboard-lead-list
plan: 01
type: execute
wave: 1
depends_on:
  - 05-02
files_modified:
  - app/page.tsx
  - components/dashboard/lead-list-dashboard.tsx
  - components/dashboard/lead-status-badge.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-04
  - LIST-06

must_haves:
  truths:
    - "The home route renders a real dashboard screen instead of scaffold copy"
    - "The lead list loads from GET /api/leads and renders name, segment, and status badge"
    - "Operational filters flow into the route as query params"
  artifacts:
    - path: "app/page.tsx"
      provides: "Dashboard entry route"
      contains: "LeadListDashboard"
    - path: "components/dashboard/lead-list-dashboard.tsx"
      provides: "Dashboard lead list shell"
      contains: "requestLeads"
    - path: "components/dashboard/lead-status-badge.tsx"
      provides: "Shared status badge renderer"
      contains: "LeadStatusBadge"
---

<objective>
Implement the first dashboard screen and connect it to the internal leads API.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace the scaffold homepage with the lead dashboard</name>
  <files>app/page.tsx, components/dashboard/lead-list-dashboard.tsx</files>
  <action>
Render a dedicated dashboard component at `/` and fetch lead data from `GET /api/leads` with uncached requests.
  </action>
</task>

<task type="auto">
  <name>Task 2: Render list items and status badges</name>
  <files>components/dashboard/lead-list-dashboard.tsx, components/dashboard/lead-status-badge.tsx</files>
  <action>
Show the basic lead card information needed for navigation: name, segment/city metadata, site presence cue, average score, and color-coded status badge.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/06-dashboard-lead-list/06-01-SUMMARY.md`
</output>
