---
phase: 06-dashboard-lead-list
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - components/dashboard/lead-list-dashboard.tsx
autonomous: true
requirements:
  - LIST-02
  - LIST-03
  - LIST-05

must_haves:
  truths:
    - "Quick filters update the visible list through the API contract"
    - "Text search narrows the list locally without a request per keystroke"
    - "The selected lead stays visually highlighted and mirrored in the preview panel"
  artifacts:
    - path: "components/dashboard/lead-list-dashboard.tsx"
      provides: "Interactive filter and selection behavior"
      contains: "useDeferredValue"
---

<objective>
Polish the list interaction model so it feels operationally useful before the richer detail panel arrives.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add quick filters and select-based API filters</name>
  <files>components/dashboard/lead-list-dashboard.tsx</files>
  <action>
Implement All / Critical / No site / Contacted quick filters and pass status, segment, and city to `GET /api/leads`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add local search and selected-lead state</name>
  <files>components/dashboard/lead-list-dashboard.tsx</files>
  <action>
Use deferred client-side search by name, keep a highlighted selected lead, and show a lightweight right-panel preview that sets up phase 7.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/06-dashboard-lead-list/06-02-SUMMARY.md`
</output>
