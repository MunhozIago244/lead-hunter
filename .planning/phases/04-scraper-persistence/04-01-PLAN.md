---
phase: 04-scraper-persistence
plan: 01
type: execute
wave: 1
depends_on:
  - 03-02
files_modified:
  - scraper/scraper.py
autonomous: true
requirements:
  - PERS-01
  - PERS-02
  - PERS-03

must_haves:
  truths:
    - "The scraper validates Supabase connectivity before doing expensive external work"
    - "Re-scrapes update scraper-owned fields without clobbering user-owned fields"
  artifacts:
    - path: "scraper/scraper.py"
      provides: "Supabase startup validation"
      contains: "def validate_supabase_connection"
---

<objective>
Harden the persistence layer so it fails fast on bad configuration and preserves user work during re-scrapes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Validate Supabase connection at startup</name>
  <files>scraper/scraper.py</files>
  <action>
Add a small startup check that confirms the service role key can read or write to the `leads` table before the scraper starts calling external APIs.
  </action>
</task>

<task type="auto">
  <name>Task 2: Protect user-owned fields during upsert</name>
  <files>scraper/scraper.py</files>
  <action>
Before upserting a lead, load the existing record and preserve:

- `pitch`
- `status`
- `contact_channel`
- `notes`

Only scraper-owned fields should be refreshed during re-scrapes.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/04-scraper-persistence/04-01-SUMMARY.md`
</output>
