---
phase: 04-scraper-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scraper/scraper.py
autonomous: true
requirements:
  - PERS-04
  - PERS-05

must_haves:
  truths:
    - "Terminal output clearly reports what happened for each processed company"
    - "The final summary distinguishes inserts, updates, and errors in a trustworthy way"
  artifacts:
    - path: "scraper/scraper.py"
      provides: "Final execution reporting"
      contains: "Concluído:"
---

<objective>
Polish the CLI execution reporting so the scraper is operationally trustworthy during real runs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Improve per-lead reporting</name>
  <files>scraper/scraper.py</files>
  <action>
Extend per-lead logs to include meaningful context such as:

- whether the lead had a website
- whether scores were collected
- whether the row was inserted or updated

Keep the output compatible with `tqdm`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Harden final summary and exit status</name>
  <files>scraper/scraper.py</files>
  <action>
Make the final summary reflect the real run:

- total discovered
- total valid leads processed
- number of new records
- number of updated records
- number of errors

Return non-zero exit code only when the run meaningfully failed.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/04-scraper-persistence/04-02-SUMMARY.md`
</output>
