---
phase: 07-dashboard-lead-detail
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - components/dashboard/lead-detail-panel.tsx
  - components/dashboard/lead-list-dashboard.tsx
autonomous: true
requirements:
  - DETL-05
  - DETL-06

must_haves:
  truths:
    - "The detail panel can mark a lead as contacted via PATCH /api/lead/[id]"
    - "The updated lead status is reflected locally without a full page reload"
    - "Loading, success, and error states are visible during async work"
  artifacts:
    - path: "components/dashboard/lead-detail-panel.tsx"
      provides: "Status action UI"
      contains: "handleMarkAsContacted"
    - path: "components/dashboard/lead-list-dashboard.tsx"
      provides: "Local lead sync after mutation"
      contains: "handleLeadUpdated"
---

<objective>
Add the first dashboard mutation flow so the user can operate directly from the detail panel.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement the contact-status action</name>
  <files>components/dashboard/lead-detail-panel.tsx</files>
  <action>
Call `PATCH /api/lead/[id]` from the detail panel, disable the button while pending, and show a friendly success or error message.
  </action>
</task>

<task type="auto">
  <name>Task 2: Keep the list and selected detail synchronized</name>
  <files>components/dashboard/lead-list-dashboard.tsx</files>
  <action>
Merge the updated lead into local state so badges and selected detail update immediately and the current filter slice stays coherent.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/07-dashboard-lead-detail/07-02-SUMMARY.md`
</output>
