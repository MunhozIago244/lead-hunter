---
phase: 05-dashboard-api-routes
plan: 01
type: execute
wave: 1
depends_on:
  - 04-02
files_modified:
  - lib/supabase/admin.ts
  - lib/api/response.ts
  - lib/leads.ts
  - app/api/leads/route.ts
  - app/api/lead/[id]/route.ts
autonomous: true
requirements:
  - API-01
  - API-03
  - API-04

must_haves:
  truths:
    - "GET /api/leads returns ordered lead data with optional filters"
    - "PATCH /api/lead/[id] only accepts user-owned mutable fields"
    - "Structured 400 / 404 / 500 responses are returned on invalid or failed requests"
  artifacts:
    - path: "app/api/leads/route.ts"
      provides: "Leads read route"
      contains: "export async function GET"
    - path: "app/api/lead/[id]/route.ts"
      provides: "Lead patch route"
      contains: "export async function PATCH"
---

<objective>
Implement the dashboard read/update API surface with strict validation and server-side Supabase access.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add a server-side Supabase admin client and response helpers</name>
  <files>lib/supabase/admin.ts, lib/api/response.ts</files>
  <action>
Create an internal helper for service-role Supabase access and a small API response helper so every route returns uncached structured JSON.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement GET and PATCH routes</name>
  <files>app/api/leads/route.ts, app/api/lead/[id]/route.ts, lib/leads.ts</files>
  <action>
Implement GET /api/leads with search and optional filters. Implement PATCH /api/lead/[id] with strict payload validation limited to `status`, `contact_channel`, `notes`, and `pitch`.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/05-dashboard-api-routes/05-01-SUMMARY.md`
</output>
