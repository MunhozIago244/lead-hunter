---
phase: 05-dashboard-api-routes
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - .env.example
  - lib/pitch.ts
  - app/api/pitch/route.ts
autonomous: true
requirements:
  - API-02
  - API-05

must_haves:
  truths:
    - "POST /api/pitch loads a lead, calls Anthropic, persists the pitch, and returns it"
    - "Prompt construction treats scraped business data as untrusted input"
  artifacts:
    - path: "app/api/pitch/route.ts"
      provides: "Pitch generation route"
      contains: "export async function POST"
    - path: "lib/pitch.ts"
      provides: "Prompt construction and Anthropic call"
      contains: "buildPitchPrompt"
---

<objective>
Implement pitch generation through Anthropic without exposing secrets or trusting scraped content as instructions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Anthropic SDK support and server-side prompt builder</name>
  <files>package.json, .env.example, lib/pitch.ts</files>
  <action>
Add the official Anthropic SDK, document `ANTHROPIC_API_KEY`, and build a prompt helper that sanitizes untrusted business data before calling the model.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement POST /api/pitch</name>
  <files>app/api/pitch/route.ts</files>
  <action>
Load the lead by id, generate a pitch with the pinned model, persist it to `leads.pitch`, and return `{ pitch }` with structured error handling.
  </action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/05-dashboard-api-routes/05-02-SUMMARY.md`
</output>
