---
phase: 03-scraper-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scraper/scraper.py
autonomous: true
requirements:
  - ANAL-04
  - ANAL-05
  - ANAL-06
  - ANAL-07
  - ANAL-08
  - ANAL-09

must_haves:
  truths:
    - "Playwright checks website signals without aborting the whole run on blocked or broken sites"
    - "WhatsApp and meta-tag presence contribute to SEO/design heuristics"
    - "Problems list is limited and ordered by impact"
  artifacts:
    - path: "scraper/scraper.py"
      provides: "Playwright analyzer"
      contains: "def analyze_with_playwright"
    - path: "scraper/scraper.py"
      provides: "Problem generation logic"
      contains: "def build_problems"
---

<objective>
Add browser-based site analysis, heuristic scoring, and partial-failure handling.

Purpose: PageSpeed alone is not enough to create compelling outreach. The scraper needs concrete signs like missing WhatsApp CTA, weak metadata, and neglected design to generate useful problems and better pitches later.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright analysis helpers</name>
  <files>scraper/scraper.py</files>
  <action>
Implement browser-based analysis for leads with a site:

- open the site with Playwright
- detect WhatsApp links or widgets
- extract title, description, and OG tags
- detect obvious block/CAPTCHA pages

If the page is blocked or broken, return null-safe analysis data rather than raising unrecoverable errors.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add design/SEO heuristics and problems list</name>
  <files>scraper/scraper.py</files>
  <action>
Combine PageSpeed and Playwright results into:

- `score_seo`
- `score_design`
- ordered `problems` list with up to 5 items

Rules should prioritize:

- no website
- poor mobile performance
- missing title/description/OG tags
- no WhatsApp CTA
- obvious outdated/weak visual signals

The scraper must continue processing the run even when one lead fails analysis.
  </action>
</task>

</tasks>

<verification>
- `python -m py_compile scraper/scraper.py`
- grep `def analyze_with_playwright` in scraper/scraper.py
- grep `def build_problems` in scraper/scraper.py
</verification>

<output>
After completion, create `.planning/phases/03-scraper-analysis/03-02-SUMMARY.md`
</output>
